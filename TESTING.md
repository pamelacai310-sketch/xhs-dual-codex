# 验证说明

## 已自动验证

- 点赞、收藏、详情常见 snake_case/camelCase 响应归一化。
- 重复笔记合并，保留更完整标题、正文、作者、标签和状态。
- `has_more`、cursor、栏目识别和安全链接处理。
- 默认导出递归去除令牌、Cookie、Authorization、编码/嵌套令牌和敏感查询参数。
- Manifest 权限白名单与 MAIN/ISOLATED 脚本配置。
- 多文件、多账号、点赞/收藏交叉去重和主题分类。
- 同一 Chrome 配置文件重复去敏导出时随机账号别名保持稳定；两个不同配置文件的别名和栏目关系不会碰撞。
- 扩展实际去敏导出格式中的 `memberships` 对接。
- 恶意文件名、路径穿越、目录内符号链接、Windows 保留名和项目输出覆盖保护。
- 项目材料及链接二次脱敏、提示注入隔离、输出后多层解码凭据审计。
- macOS/Windows 启动器的生成器 CLI 冒烟测试。

## 发布前静态检查

```sh
node --check extension/src/lib.js
node --check extension/src/injected.js
node --check extension/src/content.js
node --check extension/src/background.js
node --check extension/popup.js
node --test extension/tests/lib.test.js extension/tests/release-guards.test.js
python3 -m unittest discover -s builder/tests -v
python3 tools/package_release.py
```

最后一条命令会先重复执行全部静态检查和单元测试；任一失败都不会生成新 ZIP。随后它以固定路径、时间、普通文件模式和显式 allowlist 写包，重新核对内部逐文件 SHA，并输出 ZIP、`.zip.sha256` 与外部 `.release.json`。

## 仍需用户登录态验证

本包不包含也不能访问用户的小红书登录态，因此以下步骤必须安装后人工验证：

1. 在测试账号“收藏”采集至少跨两页的数据。
2. 在“赞过”采集至少跨两页的数据。
3. 确认正常浏览、视频播放、评论区和页面跳转没有因扩展而改变。
4. 诊断中的 XHR 或 Fetch 大于 0，最终 `has_more` 为 `false` 时才显示完整。
5. 在中途停止一次，确认状态为“部分”。
6. 切换栏目或账号，确认采集停止且不会混入另一范围。
7. 用同一笔记同时存在于收藏和赞过的账号验证去重与双重状态。
8. 先对当前已核验账号的一个栏目、3–5 篇笔记运行正文补全；确认一次只出现一个后台标签页、低频分阶段处理、完成后自动关闭。
9. 主动切到补全标签页，确认任务暂停且标签不会被关闭。
10. 遇到验证页或访问频繁，确认任务暂停且不会连续重试。
11. 分别导出默认去敏文件和私密原始归档；只把默认去敏文件交给项目生成器。
12. 在路径含空格和中文的目录运行生成器，检查项目内容和总索引。

## 重点回归场景

- 空列表、1 条、29 条、30 条、31 条和数百条。
- Fetch、XHR 与 DOM 兜底；确认 SSR/详情/推荐数据不会被误记为栏目关系。
- 重复页、重复 cursor、乱序响应和同一笔记跨账号/栏目。
- 删除、私密、视频、图文、无令牌和字段缺失的笔记。
- 页面刷新、弹窗关闭、浏览器重启、退出登录和账号切换。
- 补全后台在“已开标签但未提交正文”时终止并恢复，确认条目不会丢失或跨账号提交。
- 401/403/429、错误码 461/471/-13002、离线和超时。

DOM-only 结果无论数量多少都不能作为完整性证明。

测试夹具包含显式标注的合成账号、note ID 与 token canary，用于证明脱敏和私密归档拒绝逻辑；发布扫描器命中这些固定测试值不代表包内含真实用户数据。
